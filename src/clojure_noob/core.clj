(ns clojure-noob.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World There... I am a little girl!"))
(do (println "no prompt here!")
    (+ 1 3))
